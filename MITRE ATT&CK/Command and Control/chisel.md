# chiselとは
ネットワークトンネリングツールであり、リモートポートフォワーディングやリバーストンネリングを実現するために使用されるオープンソースのツール。

## コマンド
サーバー側の3306にアクセスすると、Chiselトンネルを通じてクライアント側の3306にアクセスする。
```bash
# サーバー側
./chisel server -p 8888 -reverse
# クライアント側
./chisel client <サーバーIP, Port> R:3306:127.0.0.1:3306 &
```
